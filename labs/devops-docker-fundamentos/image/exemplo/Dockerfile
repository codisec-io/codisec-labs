# Este é o Dockerfile que o usuário do lab vai inspecionar e buildar
# (task 1: `cat Dockerfile` e `docker build -t meu-app:1.0 .`).
# Propositalmente simples: só precisa ficar de pé com um shell disponível
# pra `docker exec` funcionar nas tasks seguintes.
FROM alpine:3.20
CMD ["sh", "-c", "tail -f /dev/null"]
